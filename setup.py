from setuptools import setup, find_packages

setup(
    name='timetraveler',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # You can list the dependencies here, for example:
        # 'requests',
        # 'flask',
    ],
)
